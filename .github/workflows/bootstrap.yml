name: GNAT Bootstrap CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-18.04
    
    steps:
    - uses: actions/checkout@v1
    - name: Hello World
      run: echo Hello World
    - name: Release
      run: mkdir -p release && touch release/bozo && touch release1 & touch release2
    - uses: actions/upload-artifact@master
      with:
        name: release
        path: release

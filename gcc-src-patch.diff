diff --git a/gcc/cfg.c b/gcc/cfg.c
index a36cdb49f..cdf1a2bcf 100644
--- a/gcc/cfg.c
+++ b/gcc/cfg.c
@@ -102,7 +102,8 @@ free_block (basic_block bb)
    bb->succs = NULL;
    vec_free (bb->preds);
    bb->preds = NULL;
-   ggc_free (bb);
+   /* patch out gcc commit ae99b31 which breaks xmlada build
+   ggc_free (bb); */
 }
 
 /* Free the memory associated with the CFG in FN.  */
